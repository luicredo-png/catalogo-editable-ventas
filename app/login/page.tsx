'use client';
import {useState,type FormEvent} from 'react';
export default function Login(){
 const [error,setError]=useState(''),[busy,setBusy]=useState(false);
 async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError('');const form=new FormData(event.currentTarget);try{const response=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({identifier:form.get('identifier'),password:form.get('password')})});if(!response.ok){setError(response.status===503?'Falta configurar el acceso seguro. Contacta al propietario.':response.status===429?'Demasiados intentos. Espera 15 minutos.':'Usuario o contraseña incorrectos.');return}location.replace('/admin')}catch{setError('No se pudo conectar. Inténtalo nuevamente.')}finally{setBusy(false)}}
 return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',background:'#081831',color:'#17243b',padding:24}}><form onSubmit={submit} style={{width:'min(100%,420px)',display:'grid',gap:20,padding:32,borderRadius:24,background:'white'}}><h1 style={{fontSize:28}}>Administrar mi catálogo</h1><label>Usuario<input style={{display:'block',width:'100%',padding:12}} name="identifier" type="text" autoComplete="username" required /></label><label>Contraseña<input style={{display:'block',width:'100%',padding:12}} name="password" type="password" autoComplete="current-password" required maxLength={128}/></label><p role="alert">{error}</p><button disabled={busy} style={{padding:16,borderRadius:12,background:'#1677ff',color:'white'}}>{busy?'Ingresando…':'Iniciar sesión'}</button><a href="/">Ver catálogo</a></form></main>;
}
