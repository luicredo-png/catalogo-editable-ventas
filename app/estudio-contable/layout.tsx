import type {Metadata} from 'next';
import './accounting.css';
export const metadata:Metadata={title:'Nexo Contable | Estrategia para crecer',description:'Servicios contables, tributarios y financieros para empresas que buscan claridad y crecimiento.'};
export default function Layout({children}:{children:React.ReactNode}){return children}
