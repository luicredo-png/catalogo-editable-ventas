import type {Metadata} from 'next';
import './accounting.css';
export const metadata:Metadata={title:'FORCHLU | Estudio contable y asesoría empresarial',description:'Asesoría empresarial, tributaria, contable y laboral para empresas que buscan claridad y crecimiento.'};
export default function Layout({children}:{children:React.ReactNode}){return children}
