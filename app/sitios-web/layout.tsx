import type {Metadata} from 'next';
export const metadata:Metadata={title:'SitioWeb.shop | Sitios web editables para negocios',description:'Páginas web profesionales con subdominio y panel administrador para cada cliente.'};
export default function Layout({children}:{children:React.ReactNode}){return children}
