# REVOLT Perú — copia recuperada

Copia del Worker de producción que sirve `revoltperu.shop`, recuperada el 22-09-2026.

## Publicación

```bash
npm install
npm run deploy
```

El proyecto conserva el binding D1 `DB` y los recursos públicos del catálogo. No cambies el `database_id`.

